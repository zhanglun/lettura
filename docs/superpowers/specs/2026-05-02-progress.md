# Lettura 实施进度追踪

**最后更新**: 2026-05-02 (Error handling + StarredOrganizeBar tests)
**总进度**: Phase 1-5.5 全部完成 + 错误处理升级 + 12 项新测试；Phase 5 验证部分完成

---

## Phase 0: 文档更新 ✅

- [x] 审查设计文档 vs 最新审计结果
- [x] 识别 5 个差异（过时 item 1.2、缺 i18n、缺代码质量、缺 bug 修复、评分不准）
- [x] 更新设计文档为七阶段计划
- [x] 创建 progress.md
- [x] 将最新 5 个 review findings 转成执行计划：`docs/superpowers/plans/2026-05-02-page-capability-finding-fix-plan.md`
- [x] 完成计划自审并更新执行顺序

## Phase 1: Review Finding P1 修复

- [x] F3 修复 `add_tag_to_article` 新建标签后绑定错误 (`src-tauri/src/feed/tag.rs`)
  - 现状：创建新 tag 后使用 `.returning(schema::tags::id)` 获取新 id，不再错误查询 tags 表第一条。
- [x] F1 后端支持 starred collection/tag/archive/notes 查询参数
  - 现状：`ArticleFilter`、HTTP handler、SQL conditions 已支持 `collection_uuid`、`tag_uuid`、`is_archived`、`has_notes`；`ArticleQueryItem` 和 SELECT 已返回 `starred_at`、`is_archived`、`notes`。
- [x] F1 前端 Starred collection/tag 选择触发真实筛选
  - 已完成：StarredPage 会把 `activeCollection`、`activeTag` 传给 `useArticle`，SWR key 会随参数变化。
  - 已完成：切换 filter/collection/tag 时自动清空失效 `selectedArticle`。
  - 已完成：Starred API 错误改为 `showErrorToast` 弹出用户可见提示。
  - 已完成：StarredOrganizeBar 详情面板工具条（见 Phase 5.5）。
- [x] F2 TopicDetail 支持 `/local/topics/:uuid` 直达/刷新加载
  - 现状：后端 `get_topic_detail_by_id` 已支持 id 或 uuid；前端会在没有 `topicId` 时调用 `fetchTopicDetail(uuid)`，并且空状态已改为仅在缺少 `uuid` 时显示。

## Phase 2: Review Finding P2 修复

- [x] F4 修复 Starred 与 Read Later 混用，并实现独立 Read Later 能力
  - 现状：新增 `articles.is_read_later`，新增 `/api/articles/{uuid}/read-later`，`StarAndRead` 增加独立 Read Later 按钮。
  - 查询语义：`All Starred` 查询 `is_starred=1`；`Read Later` 查询 `is_read_later=1` 且不强制 `is_starred=1`。Star 操作不再自动进入 Read Later。
- [x] F5 SearchPage 直达时主动加载 topics
  - 现状：SearchPage 使用窄 selector 订阅 `topics/fetchTopics`，mount 时 `topics.length === 0` 会调用 `fetchTopics`。
- [x] F5 Search 右侧 related topics 按 query/title/description 派生
  - 现状：SearchPage 会按 query 对 topic title/description 打分排序；query 为空或无匹配时回退到前 5 个 active topics。

## Phase 2.5: Read Later 独立能力

- [x] 数据库字段：`articles.is_read_later INTEGER NOT NULL DEFAULT 0`
- [x] Rust 查询：`ArticleFilter.is_read_later` + SQL condition + list/detail SELECT 返回该字段
- [x] HTTP API：`POST /api/articles/{uuid}/read-later`
- [x] Frontend dataAgent：`updateArticleReadLaterStatus`
- [x] Article toolbar：`StarAndRead` 增加独立 Read Later/Remove from Read Later 按钮
- [x] Starred 页面：Read Later filter 使用 `is_read_later=1`，不再复用 `starred && is_archived=0`
- [x] 测试：补 read later API/query/filter/button 行为覆盖
  - 后端：`test_update_article_read_later_status` ✅
  - 前端：`ReadLaterFilter.test.tsx` + `StarAndRead.test.tsx` ✅

## Phase 3: Focused Tests

- [x] 后端测试：新 tag 绑定到正确 tag (`feed::tag::tests::test_add_new_tag_to_article` + `test_add_existing_tag_to_article`)
- [x] 后端测试：collection/tag/archive/notes article filters (`feed::article::tests::test_article_filters`)
- [x] 后端测试：read later 状态更新与 `is_read_later=1` 查询 (`feed::article::tests::test_update_article_read_later_status`)
- [x] 前端测试：Starred filters 映射请求参数 (`Starred/__tests__/StarredFilters.test.tsx` — 5 tests)
- [x] 前端测试：Starred Read Later filter 不发送 `is_starred=1`，发送 `is_read_later=1` (`Starred/__tests__/ReadLaterFilter.test.tsx` — 3 tests)
- [x] 前端测试：StarAndRead Read Later 按钮调用 `updateArticleReadLaterStatus` (`Article/__tests__/StarAndRead.test.tsx` — 4 tests)
- [x] 前端测试：Topic detail 直达触发 uuid loading (`Topics/__tests__/TopicDetailDirectUrl.test.tsx` — 3 tests)
- [x] 前端测试：Search mounted directly loads topics (`Search/__tests__/SearchTopicsLoading.test.tsx` — 2 tests)

**验证**: `cargo test` 92 passed (3 pre-existing failures), `npx vitest run` 246 passed (30 files)

## Phase 4: 原七阶段遗留项

- [x] InlineReader star onClick (`Intelligence/InlineReader.tsx`)
- [x] ArticleCol goNext 边界 bug (`Article/ArticleCol.tsx`)
- [x] DailyStatus analyzed 数据 (`Intelligence/DailyStatus.tsx`) — 已修复：analyzed 显示 signal_count
- [x] View.tsx "返回" i18n (`Article/View.tsx`)
- [x] Rail.tsx 主题色 (`components/layout/Rail.tsx`)
- [x] SignalCard ref 重置 (`Intelligence/SignalCard.tsx`)
- [ ] Starred/Search/TopicEmptyPreview i18n 完整清理
- [x] 死代码、console.log、类型修复
  - View.tsx MoreHorizontal 死按钮已移除
  - 3 个 Search 死文件已删除 (Result.tsx, ResultItem.tsx, useInfiniteScroll.ts)
  - 5 处 console.log 已清理 (ArticleCol 4处, Article/index 1处)
  - StarAndRead.test.tsx mock 缺失字段修复 (author/image/media_object)
  - 残留：68 处 console.log 在其他 36 文件（pre-existing，非本轮引入）

## Phase 5.5: StarredOrganizeBar 详情面板工具条

- [x] 后端：`GET /api/articles/{id}/collections` 端点
  - `feed/collection.rs` 新增 `get_article_collections(article_id)` — JOIN `article_collections` + `collections`
  - `server/handlers/collection.rs` 新增 handler + 注册路由
- [x] 前端 API：`helpers/starredApi.ts` 新增 `getArticleCollections(articleId)`
- [x] React 组件：`layout/Starred/StarredOrganizeBar.tsx`（299 行）
  - FolderPlus Popover：Checkbox 列表切换收藏夹 + 创建新收藏夹输入框
  - Tags Popover：当前标签可移除 + 添加标签输入框
  - `article.id` 不存在时按钮禁用
  - Popover `onOpenChange` 加载数据，操作后 `onRefresh` 通知父组件
- [x] StarredPage 集成：View 外包裹 flex-col div + StarredOrganizeBar
- [x] i18n：7 个 `starred.organize.*` key（en.json + zh.json）
- [x] Archive 按钮跳过（无后端 toggle 端点）
- [x] 错误处理升级：`console.error` → `showErrorToast`（StarredOrganizeBar 6 处 + StarredPage 5 处）
- [x] 测试：StarredOrganizeBar 组件测试（12 tests）
  - Collection popover 加载 collections + article collections
  - 勾选/取消收藏夹调用 add/remove API
  - Tag popover 加载 tags
  - 新增/删除 tag 调用对应 API
  - API 失败触发 showErrorToast（4 个失败场景）
  - 无 article id 时按钮禁用

**验证**: `tsc --noEmit` 0 errors ✅, `vitest run` 258 passed (31 files) ✅

## Phase 5: 验证

- [x] `tsc --noEmit` — 0 errors ✅
- [x] `vitest run` — 258 passed (31 files) ✅
- [ ] `pnpm build`
- [ ] `cargo test`
- [ ] LSP diagnostics 清洁
- [ ] i18n key 完整性
- [ ] 补齐 Starred 失败场景测试：StarredPage 创建收藏夹失败、StarredOrganizeBar 创建收藏夹失败、删除标签失败

---

## 问题日志

| 时间 | Phase | 问题 | 解决方案 | 状态 |
|------|-------|------|----------|------|
| 2026-05-02 | Review | Starred collection/tag filters 只是 UI 状态 | 增加 article query 参数并让 `useArticle` 接收过滤条件 | 已完成：请求链路 + 响应字段 + StarredOrganizeBar 操作入口 ✅ |
| 2026-05-02 | Review | Topic detail 依赖 topics 列表，直达失败 | 增加 uuid-based detail loading | 已完成 + 已补测试 ✅ |
| 2026-05-02 | Review | 新建 tag 关联逻辑可能取错 tag | 插入后按新 tag id 关联 | 已完成 + 已补测试 ✅ |
| 2026-05-02 | Product | Star 操作会让文章出现在 Read Later，Starred/Read Later 语义混用 | 新增独立 `is_read_later` 字段、API、按钮；Read Later filter 查询 `is_read_later=1` | 已完成 + 已补测试 ✅ |
| 2026-05-02 | Review | Search related topics 不主动加载 | Search mount 时窄选择器加载 topics，并按 query 派生 | 已完成 + 已补测试 ✅ |

## 验证记录

| 时间 | 命令 | 结果 | 备注 |
|------|------|------|------|
| 2026-05-02 | `pnpm --dir apps/desktop exec tsc --noEmit` | 通过 | 无输出 |
| 2026-05-02 | `cargo check` (`apps/desktop/src-tauri`) | 通过 | 有既有 unused/dead_code warnings |
| 2026-05-02 | `cargo test` (`apps/desktop/src-tauri`) | 92 passed, 3 pre-existing failures | 失败项: ranking::test_topic_relevance, why_it_matters::test_build_wim_prompt, channel::test_fetch_site_favicon |
| 2026-05-02 | `npx vitest run` (`apps/desktop`) | 258 passed, 31 files | 含新增 29 tests (6 前端测试文件，含 StarredOrganizeBar 12 tests) |
| 2026-05-02 | `cargo check` (StarredOrganizeBar 后端) | 0 errors | 47 pre-existing warnings |
| 2026-05-02 | `tsc --noEmit` (StarredOrganizeBar 前端) | 0 errors | 含 StarredOrganizeBar.tsx + index.tsx 集成 |
| 2026-05-02 | `tsc --noEmit` (error handling upgrade) | 0 errors | StarredOrganizeBar + StarredPage showErrorToast |
| 2026-05-02 | `npx vitest run` (StarredOrganizeBar tests) | 12 passed | Collection/tag load, toggle, add/remove, error toast, disabled state |
| 2026-05-02 | Review follow-up | 已修复 | StarredPage 右侧建议区创建收藏夹失败从空 catch 改为 showErrorToast；失败场景测试仍待补齐 |
